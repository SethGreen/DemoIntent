<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.algonquincollege.hurdleg.demointent" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".PlainTextActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- TODO STEP ONE
             Important: each activity must be listed here, in the manifest file.
             Un-comment the following activity -->
        <!-- REMOVE THIS START OF COMMENT
        <activity
            android:name=".CapitalizedTextActivity"
            android:label="@string/app_name">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".PlainTextActivity" />
        </activity>
        REMOVE THIS END OF COMMENT -->
    </application>

</manifest>
